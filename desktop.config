sudo apt get install git
sudo apt install virtualbox
sudo snap install pycharm-community --classic

# Grub loadsan OS after a timeout, instead of waiting
#sudo pico /etc/default/grub
sudo update-grub
